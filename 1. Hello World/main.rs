fn main() {
    println!("Hello, World!!");
    // ! is a macro.
    // macro don't follow the same rule ass function
    // we end the line with ; which means an expression is ended.
}

// main() is special and it has to be made.
